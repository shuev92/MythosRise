name: Build MythosRise APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Extract Project Files
        run: |
          # Сваляме и разархивираме всичко стъпка по стъпка
          wget "https://github.com/shuev92/MythosRise/archive/refs/heads/main.zip" -O main.zip
          unzip main.zip
          
          # Намираме вътрешния архив (този с интервалите в името) и го разархивираме
          INNER_ZIP=$(find . -name "*.zip" -not -name "main.zip" -print -quit)
          unzip "$INNER_ZIP" -d final_extract

      - name: Build APK (Stable Mode)
        run: |
          # 1. Намираме къде точно се намира проектът (където е build.gradle)
          BUILD_GRADLE_PATH=$(find . -name "build.gradle" -not -path "*/app/*" -print -quit)
          PROJECT_ROOT=$(dirname "$BUILD_GRADLE_PATH")
          cd "$PROJECT_ROOT"
          
          echo "Проектът е намерен в: $PROJECT_ROOT"
          
          # 2. Ръчно инсталираме Gradle 7.5, за да избегнем грешката с "Gradle 10"
          wget -q https://services.gradle.org/distributions/gradle-7.5-bin.zip -O gradle_dist.zip
          unzip -q gradle_dist.zip
          export PATH=$PWD/gradle-7.5/bin:$PATH
          
          # 3. Подготвяме и стартираме компилацията
          gradle wrapper --gradle-version 7.5
          chmod +x gradlew
          ./gradlew assembleDebug -x lint --stacktrace

      - name: Upload APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: MythosRise-Final-APK
          path: "**/build/outputs/apk/debug/*.apk"
